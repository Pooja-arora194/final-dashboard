import React from 'react'
import { makeStyles } from '@material-ui/core/styles';



export const Layoutstyle = makeStyles(theme => ({
  root: {
    display: 'flex',
    
  },
    navbarWidth: theme.mixins.toolbar 

    
}));